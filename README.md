# nvim config

## TODO
- [ ] check and refine deps list
- [ ] keymaps are a mess
- [ ] tidy gitsigns and different keymaps

## dependencies:
git
curl
wget
unzip
tar
gzip
bash
neovim 
gcc (or clang)   # for treesitter
make

ripgrep          # snacks picker / search
fd               # file search
sqlite           # snacks picker DB

nodejs
npm

python
python-pip

go
tree-sitter-cli

stylua            # conform formatter

python-debugpy    # dap-python
delve             # dap-go (go debugger)

image-magick      # snacks image
ghostscript       # snacks PDF/image support

